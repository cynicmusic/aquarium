## Examples directory

- `external/` — third-party reference images (AI renders, stock photos, google images). Use as aesthetic targets.
- `internal/` — our own work: screenshots of the live aquarium + workshop, generated fish sprites, side-by-side comparisons, critic review panels.
- `reference/` — curated best-of images. Small, hand-picked subset we iterate against.
